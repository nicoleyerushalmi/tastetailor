export default function GeneratePage() {
  return (
    <main className="mx-auto flex w-full max-w-3xl flex-1 flex-col gap-3 px-4 py-10 sm:px-6">
      <h1 className="font-[family-name:var(--font-display)] text-3xl text-[var(--color-ink)]">
        Generate
      </h1>
      <p className="text-[var(--color-ink-muted)]">
        Recipe generation lands in Phase 4. Your profile is set — this is where
        adapt and scratch flows will live.
      </p>
    </main>
  );
}
