export default function GeneratePlaceholderPage() {
  return (
    <main className="mx-auto flex min-h-full max-w-lg flex-col gap-3 px-6 py-16">
      <h1 className="font-[family-name:var(--font-display)] text-3xl text-[var(--color-ink)]">
        Generate
      </h1>
      <p className="text-[var(--color-ink-muted)]">
        You are signed in. Recipe generation lands in Phase 4.
      </p>
    </main>
  );
}
